# ARIA Core — Python dependencies
# Install: pip install -r requirements.txt

# Core server
aiohttp>=3.9.0
structlog>=24.0.0
websockets>=15.0.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Digital twin (CAD + FEA + mesh)
meshio>=5.3.0
gmsh>=4.11.0
cadquery>=2.4.0
trimesh>=4.0.0

# Data + I/O
netCDF4>=1.6.0
astropy>=6.0.0
pydantic>=2.0.0
msgpack>=1.0.0
pyyaml>=6.0.0
jsonschema>=4.0.0
httpx>=0.24.0

# LLM cognitive engine (optional — falls back to rules if missing)
anthropic>=0.20.0

# Testing
pytest>=7.0.0
hypothesis>=6.80.0
